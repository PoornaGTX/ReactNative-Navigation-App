@react-navigation/native package is used to get navigation in react native

next - npm install react-native-screens react-native-safe-area-context 

npx react-native start --reset-cache for if not working navigation

next -   @react-navigation/native-stack

to get image with internet - <Image source={{ uri: imageUrl }} />

overflow: "hidden" ----- //make user that content not going outside in container

screen components has route and navigation

useLayoutEffect() ---- this is also like useEffect same process. but we use useLayoutEffect() in some senario becuse api 
screen eka update karnakota kalin thibba value eka penila itaa passe ne useEffect eka value eka watenne
enisa thama useLayoutEffect() use karanne eka nethi wenna.

Text -- cant accept the broder property

import { Ionicons } from "@expo/vector-icons"; -- for icons


*** expo doctor --fix-dependencies ** --- to fix dependencys

justifyContent:'center' // center the containt vertically

 alignItems: "center", //align items in horizontolly